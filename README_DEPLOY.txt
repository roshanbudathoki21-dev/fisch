BrainrotShop FINAL deploy package

Upload the contents of this folder to your hosting root. It includes the main index.html, real clean product URL folders like /aura-chests/index.html, sitemap.xml, robots.txt, and Vercel clean URL settings.

For product URLs like https://brainrotshop.fun/aura-chests to work with product-specific SEO metadata, deploy the whole folder, not only the single HTML file.
